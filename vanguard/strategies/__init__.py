"""
vanguard.strategies — V5 Strategy Router scoring functions.

Each strategy extends BaseStrategy and implements:
    score(df: pd.DataFrame, direction: str, top_n: int) -> pd.DataFrame

Location: ~/SS/Vanguard/vanguard/strategies/__init__.py
"""
